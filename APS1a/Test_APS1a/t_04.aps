[
    VAR w int;   
    VAR t int;
    PROC REC loop [var y: int, var x:int] [
      WHILE (not(lt y 1)) [
        SET y (sub y 1);
        ECHO y
      ]
    ];
    SET t 20;
    SET w 1;

    CALL loop (adr w) (adr t);
    ECHO w
]