[
    CONST w int 8;    
    VAR t int ;
    SET t 10;
    PROC loop [n: int, y: int] [
    IF (eq n 0) [
      SET n 110;
      ECHO n
     ] [
      WHILE (not(lt y 5)) [
        SET y (sub y 1);
        ECHO y
      ]
     ]
    ];

    SET w 10;

    CALL loop t w
]